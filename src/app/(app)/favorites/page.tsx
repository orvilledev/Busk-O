import { FavoritesScreen } from "@/components/songs/favorites-screen";

// Local-first: renders instantly from the IndexedDB mirror.
export default function FavoritesPage() {
  return <FavoritesScreen />;
}
